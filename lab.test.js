test('adds 1 + 2 to equal 3', () => {
    expect(1+2).toBe(3);
  });
  test('adds 1 + 2 to equal 3', () => {
    expect(4+5).toBe(9);
  });
  test('adds 1 + 2 to equal 3 wrong', () => {
    expect(4+5).toBe(2);
  });